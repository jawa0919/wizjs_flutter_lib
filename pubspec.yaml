name: wizjs_flutter_lib
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # webview插件
  webview_flutter: ">=2.0.0 <3.0.0"
  # 设备信息
  device_info_plus: ">=2.0.0 <3.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
